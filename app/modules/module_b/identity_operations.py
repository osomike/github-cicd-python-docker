"""
This is a module to perform identity operations
"""

def identity(value_a):
    """
        Identity method, returns the same object

    Args:
        value_a (object): An object

    Returns:
        The same object
    """
    return value_a
